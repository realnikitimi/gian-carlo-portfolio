'use client'

import { Background, Header, Loading } from '~/components'

const Projects = () => {
    return (
        <Background>
            <Header />
            <Loading />
        </Background>
    )
}

export default Projects
